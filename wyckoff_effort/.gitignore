# Test run outputs (regenerated on each run)
rl_results/

# Pipeline outputs (regenerated by running feature pipeline)
pipeline_output/

# Raw data (symlinked from external, too large for git)
datasets/*.scid
